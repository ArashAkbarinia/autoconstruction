%YAML:1.0
True positives: 737
True negatives: 351
False positives: 0
False negatives: 712
TPR: 5.0862663906142169e-01
TNR: 1.
FPR: 0.
FNR: 4.9137336093857831e-01
Accuracy: 6.0444444444444445e-01
Error rate: 3.9555555555555555e-01
Precision: 1.
Outliers: 0.
center:
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
   - { length:0, points:[] }
